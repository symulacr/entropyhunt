# Deep Interview Context Snapshot — discover-codebase

- Task statement: Discover the codebase, its purpose, completeness score, gaps, limits, and risks.
- Desired outcome: Evidence-backed assessment of what the repository currently is, how complete it is, and what is still missing or risky.
- Stated solution: Use deep-interview mode to inspect the brownfield codebase before asking the highest-leverage clarification question.
- Probable intent hypothesis: The user wants a fast but rigorous repo due-diligence pass to understand readiness, likely relative to the hackathon brief in `entropy_hunt.md`.
- Known facts/evidence:
  - Python project with modules under `auction/`, `core/`, `roles/`, `simulation/`, `viz/`, entrypoint `main.py`.
  - Product brief in `entropy_hunt.md` describes a 5-drone entropy-driven search-and-rescue swarm with BFT zone auctions and optional Webots integration.
  - Current implementation includes a stub simulation (`simulation/stub.py`), ASCII heatmap (`viz/heatmap.py`), browser mockups (`entropy_hunt_mockup.html`, `entropy_hunt_v2.html`), Python tests, and Node UI tests.
  - Verification so far: `pytest -q`, `node --test entropy_hunt_ui.test.mjs`, `ruff check .`, and `mypy .` all pass.
- Constraints:
  - Deep-interview mode must ask one question per round and avoid direct implementation.
  - Assessment should be based on repository evidence, not speculation.
- Unknowns/open questions:
  - What baseline should define the “completeness score”: the full hackathon/Webots+Vertex vision, or the current pure-stub demo scope?
  - Whether the user wants a product-readiness score, engineering-completeness score, or submission-readiness score.
- Decision-boundary unknowns:
  - Whether OMX may infer the scoring rubric from `entropy_hunt.md` or should use another standard.
- Likely codebase touchpoints:
  - `entropy_hunt.md`, `main.py`, `simulation/stub.py`, `core/*`, `roles/*`, `auction/protocol.py`, `viz/heatmap.py`, `tests/*`, `entropy_hunt_v2.html`, `entropy_hunt_ui.test.mjs`.
